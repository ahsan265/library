import { CreatefirstletterPipe } from './createfirstletter.pipe';

describe('CreatefirstletterPipe', () => {
  it('create an instance', () => {
    const pipe = new CreatefirstletterPipe();
    expect(pipe).toBeTruthy();
  });
});
