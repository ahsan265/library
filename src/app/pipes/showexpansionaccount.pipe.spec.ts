import { ShowexpansionaccountPipe } from './showexpansionaccount.pipe';

describe('ShowexpansionaccountPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowexpansionaccountPipe();
    expect(pipe).toBeTruthy();
  });
});
