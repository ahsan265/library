import { ShowemailorphonePipe } from './showemailorphone.pipe';

describe('ShowemailorphonePipe', () => {
  it('create an instance', () => {
    const pipe = new ShowemailorphonePipe();
    expect(pipe).toBeTruthy();
  });
});
